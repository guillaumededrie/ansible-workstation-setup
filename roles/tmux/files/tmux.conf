set -g default-terminal "screen-256color" 

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1

set -g history-limit 10000

# Terminal emulator window titles on distant server
set -g set-titles on
set -g set-titles-string "#T"
